import TableItemClients from "./components/TableItemClients";

export const TableItem = ({info}) => {
    
    return (
        <tr>
            <TableItemClients info={info} />
        </tr>
    )
}
